services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - .:/app
    depends_on:
      - redis
    command: gunicorn backend.wsgi:application --bind 0.0.0.0:8000 --workers 2 --reload

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
